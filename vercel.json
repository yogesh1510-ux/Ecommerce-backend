{
    "version": 2,
    "builds": [
      {
        "src": "api/**/*",
        "use": "@vercel/node",
        "config": { "engines": { "node": "14.x" } }
      }
    ],
    "routes": [
      { "src": "/api/(.*)", "dest": "/app.js" }
    ]
  }
  